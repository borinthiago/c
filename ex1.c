#include <stdio.h>

// biblioteca 

int main () // função principal
{ 
// declaração variáveis
    int n;
    float x, pi=3.14;
    unsigned int contator = 0; //unsigned só aceita positivo
    char letra;
    n = -1;
    x = 2.3;
    letra = 'A';

    printf("o valor de x= %f\n", x); //para imprimir uma variavel usamos o % %d é para inteiro
// \n é para final de linha


// programa em c

return 0;
}